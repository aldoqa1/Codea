import { StyleSheet } from "react-native"

export default StyleSheet.create({
    footerText: {
        textAlign:"center", 
        marginTop: 10, 
        marginBottom:120
    },
    footerLink: { 
        fontWeight:'bold',
        marginBottom: -4
    }
});

